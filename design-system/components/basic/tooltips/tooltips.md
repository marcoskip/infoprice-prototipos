# Tooltips

Balões de ajuda com 2 posições (acima/abaixo). Background gray-900, texto branco 12px, sem animação de entrada.

## Markup canônico

Veja `tooltips.html` — contém os snippets canônicos de cada variante (extraídos dos blocos `<pre><code>` do design-system.html).

## Estilos

Veja `tooltips.css` para as classes CSS deste componente.

## React

(a fazer — adicionar `tooltips.react.tsx` apenas se o componente exigir comportamento stateful complexo. Caso contrário, a skill `/handoff` gera React on-the-fly a partir do HTML)

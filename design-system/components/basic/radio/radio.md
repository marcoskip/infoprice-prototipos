# Radio

Seleção única obrigatória em um grupo. 3 variantes: only radio, radio + label, radio + label + texto complementar.

## Markup canônico

Veja `radio.html` — contém os snippets canônicos de cada variante (extraídos dos blocos `<pre><code>` do design-system.html).

## Estilos

Veja `radio.css` para as classes CSS deste componente.

## React

(a fazer — adicionar `radio.react.tsx` apenas se o componente exigir comportamento stateful complexo. Caso contrário, a skill `/handoff` gera React on-the-fly a partir do HTML)

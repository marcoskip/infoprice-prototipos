# Checkbox

Seleção múltipla ou opcional. 3 estados (off, on, half) e 3 variantes (caixa, + label, + label + complementar).

## Markup canônico

Veja `checkbox.html` — contém os snippets canônicos de cada variante (extraídos dos blocos `<pre><code>` do design-system.html).

## Estilos

Veja `checkbox.css` para as classes CSS deste componente.

## React

(a fazer — adicionar `checkbox.react.tsx` apenas se o componente exigir comportamento stateful complexo. Caso contrário, a skill `/handoff` gera React on-the-fly a partir do HTML)

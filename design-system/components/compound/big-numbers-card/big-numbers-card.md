# Big number card

Cards de KPI com 3 variantes: azul fixo (largura 140px), azul auto (livre) e verde (com borda destaque).

## Markup canônico

Veja `big-numbers-card.html` — contém os snippets canônicos de cada variante (extraídos dos blocos `<pre><code>` do design-system.html).

## Estilos

Veja `big-numbers-card.css` para as classes CSS deste componente.

## React

(a fazer — adicionar `big-numbers-card.react.tsx` apenas se o componente exigir comportamento stateful complexo. Caso contrário, a skill `/handoff` gera React on-the-fly a partir do HTML)

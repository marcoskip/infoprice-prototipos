# Title bar

Barra de título da página, com badge de "última atualização" e botões de ação.

## Markup canônico

Veja `title-bar.html` — contém os snippets canônicos de cada variante (extraídos dos blocos `<pre><code>` do design-system.html).

## Estilos

Veja `title-bar.css` para as classes CSS deste componente.

## React

(a fazer — adicionar `title-bar.react.tsx` apenas se o componente exigir comportamento stateful complexo. Caso contrário, a skill `/handoff` gera React on-the-fly a partir do HTML)

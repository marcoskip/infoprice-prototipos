# Title dropdown

Dropdown que abre dos botões da title bar. Border 1px gray-300, shadow-elevation, item ativo em azul.

## Markup canônico

Veja `title-dropdown.html` — contém os snippets canônicos de cada variante (extraídos dos blocos `<pre><code>` do design-system.html).

## Estilos

Veja `title-dropdown.css` para as classes CSS deste componente.

## React

(a fazer — adicionar `title-dropdown.react.tsx` apenas se o componente exigir comportamento stateful complexo. Caso contrário, a skill `/handoff` gera React on-the-fly a partir do HTML)

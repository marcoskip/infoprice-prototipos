# Cabeçalho de resultados

Barra entre filtros e grid. Estrutura obrigatória: <code style="font-family:'JetBrains Mono',monospace;font-size:13px">.cabecalho__inner</code> com <code style="font-family:'JetBrains Mono',monospace;font-size:13px">.cabecalho__info</code> (esquerda) + <code style="font-family:'JetBrains Mono',monospace;font-size:13px">.cabecalho__buttons</code> (direita).

## Markup canônico

Veja `cabecalho.html` — contém os snippets canônicos de cada variante (extraídos dos blocos `<pre><code>` do design-system.html).

## Estilos

Veja `cabecalho.css` para as classes CSS deste componente.

## React

(a fazer — adicionar `cabecalho.react.tsx` apenas se o componente exigir comportamento stateful complexo. Caso contrário, a skill `/handoff` gera React on-the-fly a partir do HTML)

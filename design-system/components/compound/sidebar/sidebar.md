# Sidebar

Menu lateral colapsavel — 52px (colapsado) ↔ 220px (expandido). Sempre 5 módulos do IPA. Tooltips no estado colapsado, labels no expandido.

## Markup canônico

Veja `sidebar.html` — contém os snippets canônicos de cada variante (extraídos dos blocos `<pre><code>` do design-system.html).

## Estilos

Veja `sidebar.css` para as classes CSS deste componente.

## React

(a fazer — adicionar `sidebar.react.tsx` apenas se o componente exigir comportamento stateful complexo. Caso contrário, a skill `/handoff` gera React on-the-fly a partir do HTML)

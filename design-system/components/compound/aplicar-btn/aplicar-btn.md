# Aplicar preço

Botão verde de destaque. Estrutura: <code style="font-family:'JetBrains Mono',monospace;font-size:13px">aplicar-btn__main</code> (ação) + <code style="font-family:'JetBrains Mono',monospace;font-size:13px">aplicar-btn__drop</code> (chevron com radio de variantes).

## Markup canônico

Veja `aplicar-btn.html` — contém os snippets canônicos de cada variante (extraídos dos blocos `<pre><code>` do design-system.html).

## Estilos

Veja `aplicar-btn.css` para as classes CSS deste componente.

## React

(a fazer — adicionar `aplicar-btn.react.tsx` apenas se o componente exigir comportamento stateful complexo. Caso contrário, a skill `/handoff` gera React on-the-fly a partir do HTML)

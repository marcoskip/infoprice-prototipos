# Grid de dados

Tabela de dados com <code style="font-family:'JetBrains Mono',monospace;font-size:13px">table-layout: fixed</code> e <code style="font-family:'JetBrains Mono',monospace;font-size:13px">&lt;colgroup&gt;</code> definindo larguras.

## Markup canônico

Veja `grid.html` — contém os snippets canônicos de cada variante (extraídos dos blocos `<pre><code>` do design-system.html).

## Estilos

Veja `grid.css` para as classes CSS deste componente.

## React

(a fazer — adicionar `grid.react.tsx` apenas se o componente exigir comportamento stateful complexo. Caso contrário, a skill `/handoff` gera React on-the-fly a partir do HTML)

# Header

Barra superior fixa, 52px de altura. Contém botão de menu de produtos (azul), logo, divisor e nome do produto. À direita: ajuda e dropdown de usuário.

## Markup canônico

Veja `header.html` — contém os snippets canônicos de cada variante (extraídos dos blocos `<pre><code>` do design-system.html).

## Estilos

Veja `header.css` para as classes CSS deste componente.

## React

(a fazer — adicionar `header.react.tsx` apenas se o componente exigir comportamento stateful complexo. Caso contrário, a skill `/handoff` gera React on-the-fly a partir do HTML)
